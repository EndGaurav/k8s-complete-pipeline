- hosts: all
  become: true
  tasks:
      - name: create new deployment 
        command: 
          cmd: kubectl apply -f /home/ubuntu/deployment.yml
          executable: /snap/bin/kubectl
      - name: create new service 
        command: 
          cmd: kubectl apply -f /home/ubuntu/service.yml
          executable: /snap/bin/kubectl